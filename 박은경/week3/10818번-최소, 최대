#include <iostream>
#include <algorithm>
#define MAX_N 1000001
using namespace std;

int arr[MAX_N]; 

int main() {
	int N = 1;

	cin >> N;
	
	for (int i = 0; i < N; i++) {
		cin >> arr[i];	
	}
	
	sort(arr, arr + N);

	cout << arr[0] << " " << arr[N-1] << endl;

	return 0;
}
